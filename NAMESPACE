# Generated by roxygen2: do not edit by hand
#
# This file is what `devtools::document()` will produce after
# you add the roxygen tags from the commented R files. It is
# committed to the repo but kept in sync with the @import /
# @importFrom tags in R/.
# ----------------------------------------------------------

# ---- Exports -------------------------------------------------
# Only the user-facing entry point is exported. UI / server
# builders and helpers stay internal.

export(launch_explorer)


# ---- Package-level imports (R/scTAMsExplorer-package.R) ------
# shiny is imported in full but with two exceptions: dataTableOutput
# and renderDataTable also exist in DT, and we want DT's versions.

import(DT)
import(dplyr)
import(ggplot2)
import(tidyr)


# `import(shiny, except = ...)` cannot be expressed with the
# plain @import tag, so it is generated from @rawNamespace.

import(shiny, except = c(dataTableOutput, renderDataTable))


# ---- importFrom: magrittr, base packages ---------------------

importFrom(magrittr, "%>%")
importFrom(stats, reorder)
importFrom(utils, head)
importFrom(utils, write.csv)


# ---- importFrom: Seurat / SeuratObject -----------------------

importFrom(Seurat, AverageExpression)
importFrom(Seurat, DefaultAssay)
importFrom(Seurat, DimPlot)
importFrom(Seurat, DoHeatmap)
importFrom(Seurat, DotPlot)
importFrom(Seurat, FeaturePlot)
importFrom(Seurat, FindMarkers)
importFrom(Seurat, Idents)
importFrom(Seurat, VlnPlot)
importFrom(SeuratObject, "Idents<-")


# ---- importFrom: tidyverse satellites ------------------------

importFrom(tibble, rownames_to_column)
importFrom(stringr, str_trunc)
importFrom(scales, rescale)
importFrom(rlang, .data)


# ---- importFrom: enrichment & plotting -----------------------

importFrom(enrichR, enrichr)
importFrom(ggrepel, geom_text_repel)
importFrom(patchwork, plot_annotation)


# ---- importFrom: interactive widgets -------------------------

importFrom(plotly, ggplotly)
importFrom(plotly, plotlyOutput)
importFrom(plotly, renderPlotly)

importFrom(visNetwork, renderVisNetwork)
importFrom(visNetwork, visEdges)
importFrom(visNetwork, visInteraction)
importFrom(visNetwork, visLayout)
importFrom(visNetwork, visNetwork)
importFrom(visNetwork, visNetworkOutput)
importFrom(visNetwork, visNodes)
importFrom(visNetwork, visOptions)
importFrom(visNetwork, visPhysics)


# ---- DT-specific exports already covered by import(DT) -------
# (DTOutput, datatable, formatSignif, dataTableOutput,
# renderDataTable are all exposed via `import(DT)` above.)


# ---- grid (used in regulon plot composition) ----------------

importFrom(grid, unit)
